project Hello is
    -- Folder where the code will be searched when compiling
    for Source_Dirs use ("src/**");

    for Object_Dir use "object";

    for Exec_Dir use ".";

    for Main use ("hello.adb");

    package Builder is
        for Executable ("hello.adb") use "main.exe";
    end Builder;

end Hello;